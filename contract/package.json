{
  "name": "sentinal-fhenix-contract",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network baseSepolia",
    "deploy:local": "hardhat run scripts/deploy.js --network hardhat",
    "test": "hardhat test"
  },
  "devDependencies": {
    "@cofhe/hardhat-plugin": "^0.5.2",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "@cofhe/sdk": "^0.5.2",
    "@fhenixprotocol/cofhe-contracts": "^0.1.3",
    "fhenix-confidential-contracts": "^0.1.0",
    "ethers": "^6.13.0"
  }
}
